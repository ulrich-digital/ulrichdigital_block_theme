/* =============================================================== *\
   Shared Entry Point

   Einstiegspunkt für Shared (Blocks).

   Wird nach build/shared.js kompiliert
   und im Editor wie auch im Frontend geladen.
\* =============================================================== */

import "./shared/shared";